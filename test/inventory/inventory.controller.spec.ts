import { inventory } from '../../src/\inventory\inventory.controller.ts';

describe('inventory.controller.ts', () => {
  it('should be tested properly', () => {
    expect(inventory).toBeDefined();
  });
});
