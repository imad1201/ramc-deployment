import { inventory } from '../../src/\inventory\inventory.controller.spec.ts';

describe('inventory.controller.spec.ts', () => {
  it('should be tested properly', () => {
    expect(inventory).toBeDefined();
  });
});
