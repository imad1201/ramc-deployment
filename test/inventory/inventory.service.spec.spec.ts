import { inventory } from '../../src/\inventory\inventory.service.spec.ts';

describe('inventory.service.spec.ts', () => {
  it('should be tested properly', () => {
    expect(inventory).toBeDefined();
  });
});
