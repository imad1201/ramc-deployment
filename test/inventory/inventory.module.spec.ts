import { inventory } from '../../src/\inventory\inventory.module.ts';

describe('inventory.module.ts', () => {
  it('should be tested properly', () => {
    expect(inventory).toBeDefined();
  });
});
