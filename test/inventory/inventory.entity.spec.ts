import { inventory } from '../../src/\inventory\inventory.entity.ts';

describe('inventory.entity.ts', () => {
  it('should be tested properly', () => {
    expect(inventory).toBeDefined();
  });
});
