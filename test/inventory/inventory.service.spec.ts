import { inventory } from '../../src/\inventory\inventory.service.ts';

describe('inventory.service.ts', () => {
  it('should be tested properly', () => {
    expect(inventory).toBeDefined();
  });
});
