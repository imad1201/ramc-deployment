import { leave } from '../../src/\hrms\leave\leave.controller.spec.ts';

describe('leave.controller.spec.ts', () => {
  it('should be tested properly', () => {
    expect(leave).toBeDefined();
  });
});
