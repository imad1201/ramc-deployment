import { leave-request } from '../../src/\hrms\leave\leave-request.module.ts';

describe('leave-request.module.ts', () => {
  it('should be tested properly', () => {
    expect(leave-request).toBeDefined();
  });
});
