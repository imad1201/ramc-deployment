import { update-leave } from '../../src/\hrms\leave\dto\update-leave.dto.ts';

describe('update-leave.dto.ts', () => {
  it('should be tested properly', () => {
    expect(update-leave).toBeDefined();
  });
});
