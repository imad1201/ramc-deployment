import { create-leave } from '../../src/\hrms\leave\dto\create-leave.dto.ts';

describe('create-leave.dto.ts', () => {
  it('should be tested properly', () => {
    expect(create-leave).toBeDefined();
  });
});
