import { create-leave-request } from '../../src/\hrms\leave\dto\create-leave-request.dto.ts';

describe('create-leave-request.dto.ts', () => {
  it('should be tested properly', () => {
    expect(create-leave-request).toBeDefined();
  });
});
