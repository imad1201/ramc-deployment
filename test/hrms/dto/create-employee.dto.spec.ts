import { create-employee } from '../../src/\hrms\dto\create-employee.dto.ts';

describe('create-employee.dto.ts', () => {
  it('should be tested properly', () => {
    expect(create-employee).toBeDefined();
  });
});
