import { update-leave-request } from '../../src/\hrms\dto\update-leave-request.dto.ts';

describe('update-leave-request.dto.ts', () => {
  it('should be tested properly', () => {
    expect(update-leave-request).toBeDefined();
  });
});
