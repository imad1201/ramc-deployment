import { update-attendance } from '../../src/\hrms\dto\update-attendance.dto.ts';

describe('update-attendance.dto.ts', () => {
  it('should be tested properly', () => {
    expect(update-attendance).toBeDefined();
  });
});
