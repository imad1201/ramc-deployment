import { update-employee } from '../../src/\hrms\dto\update-employee.dto.ts';

describe('update-employee.dto.ts', () => {
  it('should be tested properly', () => {
    expect(update-employee).toBeDefined();
  });
});
