import { leave-request } from '../../src/\hrms\leave-request\leave-request.service.ts';

describe('leave-request.service.ts', () => {
  it('should be tested properly', () => {
    expect(leave-request).toBeDefined();
  });
});
