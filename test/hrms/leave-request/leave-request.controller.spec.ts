import { leave-request } from '../../src/\hrms\leave-request\leave-request.controller.ts';

describe('leave-request.controller.ts', () => {
  it('should be tested properly', () => {
    expect(leave-request).toBeDefined();
  });
});
