import { attendance } from '../../src/\hrms\entities\attendance.entity.ts';

describe('attendance.entity.ts', () => {
  it('should be tested properly', () => {
    expect(attendance).toBeDefined();
  });
});
