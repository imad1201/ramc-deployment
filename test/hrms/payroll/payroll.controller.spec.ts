import { payroll } from '../../src/\hrms\payroll\payroll.controller.ts';

describe('payroll.controller.ts', () => {
  it('should be tested properly', () => {
    expect(payroll).toBeDefined();
  });
});
