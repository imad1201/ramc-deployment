import { attendance } from '../../src/\hrms\attendance\attendance.service.ts';

describe('attendance.service.ts', () => {
  it('should be tested properly', () => {
    expect(attendance).toBeDefined();
  });
});
