import { employee } from '../../src/\hrms\employee\employee.controller.ts';

describe('employee.controller.ts', () => {
  it('should be tested properly', () => {
    expect(employee).toBeDefined();
  });
});
