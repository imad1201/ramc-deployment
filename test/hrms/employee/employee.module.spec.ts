import { employee } from '../../src/\hrms\employee\employee.module.ts';

describe('employee.module.ts', () => {
  it('should be tested properly', () => {
    expect(employee).toBeDefined();
  });
});
