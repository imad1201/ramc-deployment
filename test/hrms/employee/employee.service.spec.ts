import { employee } from '../../src/\hrms\employee\employee.service.ts';

describe('employee.service.ts', () => {
  it('should be tested properly', () => {
    expect(employee).toBeDefined();
  });
});
