import { payroll } from '../../src/\hrms\repositories\payroll.repository.ts';

describe('payroll.repository.ts', () => {
  it('should be tested properly', () => {
    expect(payroll).toBeDefined();
  });
});
