import { employee } from '../../src/\hrms\repositories\employee.repository.ts';

describe('employee.repository.ts', () => {
  it('should be tested properly', () => {
    expect(employee).toBeDefined();
  });
});
