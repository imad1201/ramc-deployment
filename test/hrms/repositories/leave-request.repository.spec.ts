import { leave-request } from '../../src/\hrms\repositories\leave-request.repository.ts';

describe('leave-request.repository.ts', () => {
  it('should be tested properly', () => {
    expect(leave-request).toBeDefined();
  });
});
