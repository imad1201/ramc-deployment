import { attendance } from '../../src/\hrms\repositories\attendance.repository.ts';

describe('attendance.repository.ts', () => {
  it('should be tested properly', () => {
    expect(attendance).toBeDefined();
  });
});
