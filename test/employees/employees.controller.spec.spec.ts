import { employees } from '../../src/\employees\employees.controller.spec.ts';

describe('employees.controller.spec.ts', () => {
  it('should be tested properly', () => {
    expect(employees).toBeDefined();
  });
});
