import { employees } from '../../src/\employees\employees.module.ts';

describe('employees.module.ts', () => {
  it('should be tested properly', () => {
    expect(employees).toBeDefined();
  });
});
