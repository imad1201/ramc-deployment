import { employees } from '../../src/\employees\employees.service.spec.ts';

describe('employees.service.spec.ts', () => {
  it('should be tested properly', () => {
    expect(employees).toBeDefined();
  });
});
