import { employees } from '../../src/\employees\employees.controller.ts';

describe('employees.controller.ts', () => {
  it('should be tested properly', () => {
    expect(employees).toBeDefined();
  });
});
