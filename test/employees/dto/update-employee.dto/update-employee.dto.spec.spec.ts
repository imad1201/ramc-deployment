import { update-employee } from '../../src/\employees\dto\update-employee.dto\update-employee.dto.spec.ts';

describe('update-employee.dto.spec.ts', () => {
  it('should be tested properly', () => {
    expect(update-employee).toBeDefined();
  });
});
