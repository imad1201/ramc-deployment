import { create-employee } from '../../src/\employees\dto\create-employee.dto\create-employee.dto.spec.ts';

describe('create-employee.dto.spec.ts', () => {
  it('should be tested properly', () => {
    expect(create-employee).toBeDefined();
  });
});
