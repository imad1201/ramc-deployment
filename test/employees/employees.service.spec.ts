import { employees } from '../../src/\employees\employees.service.ts';

describe('employees.service.ts', () => {
  it('should be tested properly', () => {
    expect(employees).toBeDefined();
  });
});
