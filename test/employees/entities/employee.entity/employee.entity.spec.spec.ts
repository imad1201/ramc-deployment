import { employee } from '../../src/\employees\entities\employee.entity\employee.entity.spec.ts';

describe('employee.entity.spec.ts', () => {
  it('should be tested properly', () => {
    expect(employee).toBeDefined();
  });
});
