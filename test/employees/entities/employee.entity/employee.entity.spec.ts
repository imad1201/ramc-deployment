import { employee } from '../../src/\employees\entities\employee.entity\employee.entity.ts';

describe('employee.entity.ts', () => {
  it('should be tested properly', () => {
    expect(employee).toBeDefined();
  });
});
