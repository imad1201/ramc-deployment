import { payroll } from '../../src/\payroll\entities\payroll.entity\payroll.entity.spec.ts';

describe('payroll.entity.spec.ts', () => {
  it('should be tested properly', () => {
    expect(payroll).toBeDefined();
  });
});
