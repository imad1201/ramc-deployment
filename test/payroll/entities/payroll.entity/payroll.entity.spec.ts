import { payroll } from '../../src/\payroll\entities\payroll.entity\payroll.entity.ts';

describe('payroll.entity.ts', () => {
  it('should be tested properly', () => {
    expect(payroll).toBeDefined();
  });
});
