import { payroll } from '../../src/\payroll\payroll.controller.spec.ts';

describe('payroll.controller.spec.ts', () => {
  it('should be tested properly', () => {
    expect(payroll).toBeDefined();
  });
});
