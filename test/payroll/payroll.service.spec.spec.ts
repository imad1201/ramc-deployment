import { payroll } from '../../src/\payroll\payroll.service.spec.ts';

describe('payroll.service.spec.ts', () => {
  it('should be tested properly', () => {
    expect(payroll).toBeDefined();
  });
});
