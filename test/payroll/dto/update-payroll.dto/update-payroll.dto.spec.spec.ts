import { update-payroll } from '../../src/\payroll\dto\update-payroll.dto\update-payroll.dto.spec.ts';

describe('update-payroll.dto.spec.ts', () => {
  it('should be tested properly', () => {
    expect(update-payroll).toBeDefined();
  });
});
