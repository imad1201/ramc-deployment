import { leave-request } from '../../src/\leave-requests\entities\leave-request.entity\leave-request.entity.spec.ts';

describe('leave-request.entity.spec.ts', () => {
  it('should be tested properly', () => {
    expect(leave-request).toBeDefined();
  });
});
