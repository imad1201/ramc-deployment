import { leave-request } from '../../src/\leave-requests\entities\leave-request.entity\leave-request.entity.ts';

describe('leave-request.entity.ts', () => {
  it('should be tested properly', () => {
    expect(leave-request).toBeDefined();
  });
});
