import { create-leave-request } from '../../src/\leave-requests\dto\create-leave-request.dto\create-leave-request.dto.spec.ts';

describe('create-leave-request.dto.spec.ts', () => {
  it('should be tested properly', () => {
    expect(create-leave-request).toBeDefined();
  });
});
