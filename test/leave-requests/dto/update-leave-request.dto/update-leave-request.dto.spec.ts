import { update-leave-request } from '../../src/\leave-requests\dto\update-leave-request.dto\update-leave-request.dto.ts';

describe('update-leave-request.dto.ts', () => {
  it('should be tested properly', () => {
    expect(update-leave-request).toBeDefined();
  });
});
