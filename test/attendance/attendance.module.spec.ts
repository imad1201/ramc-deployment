import { attendance } from '../../src/\attendance\attendance.module.ts';

describe('attendance.module.ts', () => {
  it('should be tested properly', () => {
    expect(attendance).toBeDefined();
  });
});
