import { attendance } from '../../src/\attendance\attendance.service.spec.ts';

describe('attendance.service.spec.ts', () => {
  it('should be tested properly', () => {
    expect(attendance).toBeDefined();
  });
});
