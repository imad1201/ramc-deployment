import { update-attendance } from '../../src/\attendance\dto\update-attendance.dto\update-attendance.dto.spec.ts';

describe('update-attendance.dto.spec.ts', () => {
  it('should be tested properly', () => {
    expect(update-attendance).toBeDefined();
  });
});
