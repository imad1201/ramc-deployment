import { create-attendance } from '../../src/\attendance\dto\create-attendance.dto\create-attendance.dto.spec.ts';

describe('create-attendance.dto.spec.ts', () => {
  it('should be tested properly', () => {
    expect(create-attendance).toBeDefined();
  });
});
