import { attendance } from '../../src/\attendance\attendance.controller.spec.ts';

describe('attendance.controller.spec.ts', () => {
  it('should be tested properly', () => {
    expect(attendance).toBeDefined();
  });
});
