import { attendance } from '../../src/\attendance\entities\attendance.entity\attendance.entity.spec.ts';

describe('attendance.entity.spec.ts', () => {
  it('should be tested properly', () => {
    expect(attendance).toBeDefined();
  });
});
