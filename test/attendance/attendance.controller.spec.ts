import { attendance } from '../../src/\attendance\attendance.controller.ts';

describe('attendance.controller.ts', () => {
  it('should be tested properly', () => {
    expect(attendance).toBeDefined();
  });
});
